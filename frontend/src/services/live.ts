export const liveIntegrationMessage =
  'Live mode is reserved for the final ABI and deployed Sepolia addresses. Replace the placeholder ABI fragments and write paths before switching out of mock mode.';
